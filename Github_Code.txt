cd /c/Users/AshTrailer/Documents/MATLAB/Capstone_Project

git status

git add .

!git commit -m "在这里写清楚这次改了什么"

!git push


!git commit -m "Add init_template_ar: rolling template + intraday AR(p) state. Template T(s) is a pointwise rolling same-slot mean (per-slot window). Residual AR uses ONE intraday AR(p) on consecutive residuals r(t) = y(t) - T_t(s_t), updated by vector RLS (lambda 0.98) every 5 min.Cross-day per-slot regressors removed: same-slot values 24 h apart carryalmost no correlation, so all cross-day memory lives in the template. Add update_template_ar: point update of template + intraday residual AR. Order matters: capture t_pre (pre-update template) to define the residual r = y_true - t_pre (consistent with the last forecast), RLS-update phi on consecutive regressors st.hist, push r into st.hist, then roll the template window (evict oldest, insert y_true). First pass initializes T(s) with the observation itself.  Add predict_template_ar: iterated forecast of template + intraday AR(p). y_hat = T(u) + r_hat(u). r_hat is a 288-step iterated recursion from st.hist (consecutive intraday lags): short leads chase the current residual level, long leads decay toward 0 so y_hat converges to the pure template. Returns r_hat and t_vec separately for reuse by the blend predictors.  Add run_predictor_benchmark: unified predictor benchmark, 5-min cadence. Runs six predictors (persistence, rolling template, theta-y tracker,template + intraday AR(4), blend A, blend B) with update cadence = 1:every 5-min observation re-issues the full 288-step forecast, producing a 288x288 RMSE(slot, lead) matrix per predictor. Forecasts and actuals are
clamped to a rolling 30-day global [Q1,Q99] before RMSE/MAE are computed (on the signal the MPC receives). Two-week test (Feb 1-14), forecasts started Jan 31 so every target owns all 288 lead times. Outputs: week-aligned time series, RMSE vs lead time (mean/median +/- std), per-slot RMSE, and 288x288 heatmaps; console tables cover aggregate, per-lead,per-day, and per-slot RMSE plus improvement vs persistence."
